%p#notice= notice

%h1
  = "User ID:"
  = @user.id
%p= link_to 'edit', edit_user_path(@user)
%p= link_to 'delete me', @user, method: :delete

%p
  .caption First:
  = @user.first
%p
  .caption Last:
  = @user.last
%p
  .caption Email:
  = @user.email

%p= link_to 'back to user list', users_path
